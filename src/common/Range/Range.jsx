import React from 'react';
import PropTypes from 'prop-types';
//import { Test } from './Range.styles';

const Range = (props) => (
  <div className="RangeWrapper">
    Test content
  </div>
);

Range.propTypes = {
  // bla: PropTypes.string,
};

Range.defaultProps = {
  // bla: 'test',
};

export default Range;
