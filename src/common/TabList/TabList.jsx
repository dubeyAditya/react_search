import React from 'react';
import PropTypes from 'prop-types';
//import { Test } from './TabList.styles';

const TabList = (props) => (
  <div className="TabListWrapper">
    Test content
  </div>
);

TabList.propTypes = {
  // bla: PropTypes.string,
};

TabList.defaultProps = {
  // bla: 'test',
};

export default TabList;
